[package]
name = "neo-fortune"
version = "0.1.0"
authors = ["David Knaack <davidkna@users.noreply.github.com>"]
edition = "2018"

[lib]
path = "src/lib.rs"

[[bin]]
name = "neo-fortune"
path = "src/main.rs"
required-features = ["build-binary"]

[features]
default = ["build-binary"]
build-binary = [
    "ansi_term",
    "cowsay",
    "directories",
    "flate2",
    "lolcat",
    "structopt",
    "tar",
    "ureq",
]

[dependencies]
# Lib
memchr = "2.3.0"
rand = { version = "0.7.3", features = ["std", "small_rng"] }

# Binary
directories = { version = "2.0.2", optional = true }
structopt =  { version = "0.3.8", optional = true }

# Download
flate2 = { version = "1.0", optional = true, default-features = false, features = ["rust_backend"] }
ureq = { version = "0.11.3", optional = true, default-features = false, features = ["tls"] }
tar = { version = "0.4.26", optional = true }

[dependencies.cowsay]
git = "https://github.com/davidkna/cowsay-rs"
optional = true
default-features = false

[dependencies.lolcat]
git = "https://github.com/davidkna/lolcat-rs"
optional = true
default-features = false

[target.'cfg(windows)'.dependencies]
ansi_term = { version = "0.12", optional = true }
